To execute the app, one should already have npm installed. To start, download the zip file and type the following commands:

npm i
npm i express         (if express was not already installed)
npm i socket.io       (if socket.io was not already installed)
